/** Fond sombre unique pour drill-down KPI (liste + fiche) — scroll OK sans double modal Radix. */
export function DashboardDrillDownBackdrop() {
  return (
    <div
      className="fixed inset-0 z-40 bg-black/80"
      aria-hidden
    />
  );
}
