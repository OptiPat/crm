/**
 * Affichage de la licence payante (écran d’activation, panneau Paramètres, bannière).
 * Le moteur Rust (`LICENSE_UI_VISIBLE` dans `licensing/state.rs`) reste en place.
 * Remettre à `true` ici **et** côté Rust pour réafficher la facturation.
 */
export const LICENSE_UI_VISIBLE = false;
