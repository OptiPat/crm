# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Tauri
/src-tauri/target

# --- Données sensibles (ne jamais committer) ---
_fixtures/identity/*
!_fixtures/identity/.gitkeep
# RIO clients déposés localement pour diagnostic (jamais committer)
_rio_local/*
!_rio_local/.gitkeep
# Modèles Excel clients réels déposés localement pour adaptation (jamais committer)
_import_local/*
!_import_local/.gitkeep
.cursor/mcp.json
scripts/invoke-n8n-mcp*.ps1
scripts/n8n-*.json
scripts/_tmp-creds.json
scripts/_wf-out.txt
scripts/_wf.json
*.db
*.db-journal
*.db-wal
*.db-shm
auth.json
*.key
*.pem
.env
.env.*
!.env.example

# Sauvegardes locales / exports
**/backups/
*.backup
*BACKUP*
!src-tauri/src/backup.rs
!src-tauri/src/backup_sidecar.rs
*OLD_*

# Releases générées localement
release-artifacts/

# Excel (exports / modèles — regénérer avec generate-template-filleuls.ps1)
*.xlsx

# Artefacts locaux d'analyse / OCR (hors stack)
_tmp_docx_analysis/
*.traineddata
